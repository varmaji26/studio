rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // DANGER: Allow read/write access to all users, including unauthenticated ones.
      // This is not recommended for production.
      allow read, write;
    }
  }
}
