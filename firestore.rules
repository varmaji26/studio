
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to the entire database.
    // This is not recommended for production environments.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
